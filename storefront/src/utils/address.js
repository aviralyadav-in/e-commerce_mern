/**
 * Address type ke friendly labels — schema enum (HOME/WORK/OTHER) ke liye.
 * Badges/select options me reuse hota hai.
 */
export const ADDRESS_TYPE_LABELS = {
  HOME: "Home",
  WORK: "Work",
  OTHER: "Other",
};
